version: "3.8"

services:
  osdev:
    build: .
    image: os-dev-ubuntu-22-04
    container_name: os-dev
    tty: true
    stdin_open: true
    user: dev
    working_dir: /workspace/kernel

    volumes:
      - ./dependencies:/workspace/dependencies
      - ./kernel:/workspace/kernel